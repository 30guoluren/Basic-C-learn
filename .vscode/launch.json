{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (MinGW)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe", 
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false,      // 🚨 用 VSCode 内置终端，而不是外部窗口
            "MIMode": "gdb",
            "miDebuggerPath": "E:\\Compile-Setting\\C\\mingw64\\bin\\gdb.exe", // 改成你 MinGW 里的 gdb 路径
            "preLaunchTask": "C/C++: gcc.exe 生成活动文件"   // 🚨 对应你 tasks.json 里的 label
        }
    ]
}
